import 'package:flutter/material.dart';

class PageStatusWidget extends StatelessWidget {
  const PageStatusWidget ({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
